# DUMPS — the raw-verdict intake zone
The user drops EVERY platform result here (auto reviews, FP-panel reports, audit outputs, check verdicts — good or
bad), appending over time. THE LOOP: maintainer AI studies new drops -> syncs them into ~/Desktop/olympus-agent-corpus/
(indexed) -> distills GLOBAL principles into the workflow -> project AIs ADAPT those principles per-repo.
Project AIs never read DUMPS directly. Checks evolve — every study is a dated snapshot; re-verify on platform updates.
